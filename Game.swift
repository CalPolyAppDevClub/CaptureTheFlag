class Game {
    
    var id: String
    var startTime: Date
    var TeamA: Team
    var TeamB: Team

    Game() {
        //what does into initializing the game?
    }

    func playerUpdate(player: Player) {
        //what happens when a player changes?
    }

    func flagCaptured() {
        //what happens when a flag is captured?
    }
}